/**
 * Initialize Array - Literal
 * It means []
 */

// Array Literal
const arr = [];
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;
console.log(arr.length);
console.log(arr);

/**
 * length = lastIndex + 1;
 * lastIndex = length - 1;
 */

const names = [
    "mohosin hasan",
    "robin sikdar",
    "rasel mondol",
];

// save something at the end
names[names.length] = 'naim khan'